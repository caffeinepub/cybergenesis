{
  "schemaVersion": "1.0.0",
  "projectStateVersion": 3,
  "summary": "Project investigating/fixing three.js canvas transparency: background turns black after GLTF model loads when Bloom/EffectComposer is enabled; fix requested to force clear alpha each frame.",
  "architectural_notes": [
    "When using EffectComposer/UnrealBloomPass with a transparent canvas, force renderer alpha each frame (e.g., call gl.setClearAlpha(0) in useFrame) to prevent postprocessing/GLTF load from resetting clearAlpha to 1."
  ],
  "known_issues": [
    "Canvas background briefly shows site gradient, then turns black once GLTF model finishes loading when Bloom postprocessing is enabled (transparency lost)."
  ],
  "isFixRequest": true,
  "existing_features": [
    {
      "id": "feature-in-frontend-src-components-cubevisualization-tsx-inside-the-bloomeffect-component-s-useframe-callback-call-gl-setclearalpha-0-every-frame-to-force-the-webgl-renderer-to-keep-a-transparent-clear-alpha-even-after-gltf-bloom-initialization-resets-it-place-this-call-within-the-existing-if-composerref-current-block-and-ensure-it-executes-before-composerref-current-render",
      "title": "In `frontend/src/components/CubeVisualization.tsx`, inside the `BloomEffect` component’s `useFrame` callback, call `gl.setClearAlpha(0)` every frame to force the WebGL renderer to keep a transparent clear alpha even after GLTF/Bloom initialization resets it. Place this call within the existing `if (composerRef.current)` block and ensure it executes before `composerRef.current.render()`.",
      "reqIds": [
        "REQ-1"
      ],
      "version": 1
    }
  ],
  "new_feature_requests": [
    {
      "id": "AR-1",
      "summary": "Fix canvas background turning black after GLTF loads when Bloom/EffectComposer is enabled by adding gl.setClearAlpha(0) in useFrame.",
      "reqIds": [],
      "status": "in_progress"
    }
  ],
  "projectName": "Fix Three.js canvas transparency by forcing clear alpha each frame",
  "clarification_mode": "instant",
  "clarification_rounds": 0
}